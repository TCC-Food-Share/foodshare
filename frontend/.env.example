# Base da API. Em dev o Vite faz proxy de /api -> http://localhost:3000
# (ver server.proxy em vite.config.ts), então o default abaixo já funciona
# com o backend rodando em :3000. Em produção, /api é roteado para o backend
# pelo reverse proxy (mesma origem).
VITE_API_URL=/api
