DATABASE_URL="postgresql://usuario:senha@localhost:5433/foodshare?schema=public"

MINIO_ENDPOINT="localhost"
MINIO_PORT="9000"
MINIO_ACCESS_KEY="troque-me"
MINIO_SECRET_KEY="troque-me"

JWT_SECRET="gere-com-openssl-rand-base64-32"
BETTER_AUTH_URL="http://localhost:3000"

# Origens confiáveis para o originCheck do better-auth (login/logout são POST).
# Lista separada por vírgula. Em dev, a origem do Vite. Em prod, o domínio do
# frontend. Default (quando omitido): http://localhost:5173
TRUSTED_ORIGINS="http://localhost:5173"
